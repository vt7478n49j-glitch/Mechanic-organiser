Mechanic Mate v41.5 Sync Diagnostics
- Fixes unreachable post-download verification in v41.4
- Uses a longer 64-bit-style content fingerprint
- Shows job count, latest job and logo fingerprint for device/cloud
- Download re-reads localStorage and verifies persisted content
- Upload This Device uses a dedicated forced-upload path then re-fetches and verifies cloud
- Synced is only shown after content comparison
