Mechanic Mate v43
- Fixes persistent top-right Sync issue badge when status is actually Synced
- Keeps verified automatic two-device sync from v42
- Adds private Supabase Storage support for job photos
- New job photos upload to Storage when configured
- Existing embedded photos continue to display
- Adds Test photo storage and Migrate existing photos controls
- Deletes Storage object when a stored job photo is removed
- Falls back to local embedded photo data if Storage is not configured, with a warning
- Safety backups remain lightweight and omit image bytes
