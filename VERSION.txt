Mechanic Mate v43.2 Photo Scope Fix
- Fixes 'Can't find variable: cloudSession' when saving job photos
- Exposes photo-storage upload/delete/sign-in helpers safely across app script scopes
- Existing and new job photos can now use the configured Supabase Storage bucket
- Safety backups now omit both photo image bytes and the large company-logo image
- Startup cleanup keeps 4 lightweight safety backups
- Storage usage popup now uses proper line breaks
- Retains verified automatic sync and v43 private photo storage
