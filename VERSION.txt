Mechanic Mate v43.1 Photo Save Fix
- Cleans old photo-heavy safety backups left by earlier versions
- Reduces retained safety backups to 8
- Retries core local save after trimming backups if browser quota is reached
- No longer silently falls back to embedding photos in localStorage when Supabase Storage upload fails
- Keeps selected photos visible and shows the exact Storage error instead
- Adds Storage usage diagnostic button
- Retains v43 private Supabase Storage and verified automatic sync
