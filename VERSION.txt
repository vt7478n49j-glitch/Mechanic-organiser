Mechanic Mate v41.8 Upload API Fix
- Fixes v41.7 'Can't find variable: sbFetch'
- Forced upload now uses the app's existing authenticated supabaseRequest() helper
- Directly PATCHes the signed-in user's row, creates it if absent
- Re-fetches the cloud row and verifies actual content before success
- Retains manual-operation lock, diagnostics and all existing app features
