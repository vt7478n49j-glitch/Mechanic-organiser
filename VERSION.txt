Mechanic Mate v42 Auto Sync / Photo Quota Prep
- Keeps v41.8 as the verified manual-sync foundation
- Automatically checks cloud when app gains focus, becomes visible, and every 30 seconds while open
- Automatically downloads a newer cloud revision only when the local device has no unsynced changes
- Automatically pushes local changes only when cloud is still at the device's known baseline
- True divergence remains blocked as Sync issue rather than overwriting either copy
- Safety backups no longer duplicate large embedded photo image data
- Photo metadata remains in backups; image bytes are omitted to reduce localStorage quota pressure
- Supabase Storage migration is intentionally not activated until a Storage bucket/policies exist
