Mechanic Mate v41.6 Manual Sync Fix
- Locks automatic cloud activity while Upload This Device is running
- Clears pending autosave before a manual forced upload
- Uses direct PATCH against the signed-in user's Supabase row
- Creates the row if it does not exist
- Re-fetches Supabase after upload and compares exact data fingerprint
- Reports Verified upload only when device and cloud genuinely match
- Reports explicit UPLOAD FAILED / UPLOAD MISMATCH otherwise
- Retains v41.5 diagnostics and all v41.3 app features
